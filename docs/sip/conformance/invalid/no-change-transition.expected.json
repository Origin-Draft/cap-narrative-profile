{
  "level": 2,
  "description": "Transition has identical before and after states (no actual change). Also missing required 'description' field on transition. Passes basic schema if description were present, but the semantic constraint that before != after is a Level 2 check.",
  "expected_errors": [
    {
      "path": "/units/0/structure/transition",
      "rule": "required",
      "field": "description",
      "severity": "ERROR",
      "message": "'description' is a required property"
    },
    {
      "path": "/units/0/structure/transition",
      "rule": "epistemic_consistency",
      "field": "before/after",
      "severity": "WARNING",
      "message": "Transition before and after states are identical — no change recorded"
    }
  ]
}
